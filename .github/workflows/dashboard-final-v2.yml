name: Dashboard final publish (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "Disabled: pages.yml is the sole dashboard publisher."
