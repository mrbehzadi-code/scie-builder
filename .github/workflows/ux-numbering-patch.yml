name: UX numbering patch (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "Disabled: numbering is owned by the canonical dashboard renderer."
